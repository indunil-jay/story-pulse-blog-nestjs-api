import { Injectable } from '@nestjs/common';

/**
 * The `AppService` class is a provider in the NestJS application.
 * It serves as a central service for application-wide functionalities.
 */
@Injectable()
export class AppService {}
